
docker network create pg-network

docker run -it \
    -e POSTGRES_USER="root" \
    -e POSTGRES_PASSWORD="root" \
    -e POSTGRES_DB="ny_taxi" \
    -v /home/carlos/de-zoomcamp-2024-data/postgres-db:/var/lib/postgresql/data \
    -p 5432:5432 \
    --network=pg-network \
    --name pg-database \
    postgres:13


docker run -it \
    -e PGADMIN_DEFAULT_EMAIL="admin@admin.com" \
    -e PGADMIN_DEFAULT_PASSWORD="root" \
    -p 8080:80 \
    --network=pg-network \
    --name pgadmin \
    dpage/pgadmin4


pgcli -h localhost -p 5432 -u root -d ny_taxi



Q3: select count(*) from green_taxi_data where date(lpep_pickup_datetime)='2019-09-18' and date(lpep_dropoff_datetime)='2019-09-18';

Q4: select date(lpep_pickup_datetime), max(trip_distance) from green_taxi_data group by date(lpep_pickup_datetime) order by 2 desc;

Q5: select b."Borough", sum(a.total_amount) from green_taxi_data a inner join taxi_zone_lookup b on a."PULocationID" = b."LocationID" group by b."Borough" having sum(a.total_amount) > 50000 order by 2 desc;

Q6: select b."Zone", max(a.tip_amount) from green_taxi_data a inner join taxi_zone_lookup b on a."DOLocationID" = b."LocationID" where b."Zone" in ('Central Park', 'Jamaica', 'JFK Airport', 'Long Island City/Queens Plaza') group by b."Zone" order by 2 desc;


python upload-data.py \
    --user=root \
    --password=root \
    --host=localhost \
    --port=5432 \
    --db=ny_taxi \
    --table_name=green_taxi_data \
    --url="https://github.com/DataTalksClub/nyc-tlc-data/releases/download/green/green_tripdata_2019-09.csv.gz"


docker build -t taxi-ingest:v1 .


docker run -it \
    --network=pg-network \
    --name taxi-ingest \
    taxi-ingest:v1 \
        --user=root \
        --password=root \
        --host=pg-database \
        --port=5432 \
        --db=ny_taxi \
        --table_name=green_taxi_data \
        --url="https://github.com/DataTalksClub/nyc-tlc-data/releases/download/green/green_tripdata_2019-09.csv.gz"